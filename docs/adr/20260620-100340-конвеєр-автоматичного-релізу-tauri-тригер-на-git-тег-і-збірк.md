---
session: 1f2895bd-6329-4038-b7c6-4fa942487719
captured: 2026-06-20T10:03:40+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-vitaliytv-mlmail/1f2895bd-6329-4038-b7c6-4fa942487719.jsonl
---

## ADR Конвеєр автоматичного релізу Tauri: тригер на git-тег і збірка DMG + APK

## Context and Problem Statement
Проєкти mlmail і myshare (Tauri 2, bun-монорепо) не мали автоматизованого релізу: артефакти DMG (macOS universal) і APK (Android підписаний) доводилося збирати та завантажувати вручну. Потрібен механізм, що запускається по git-тегу і завантажує обидва файли в GitHub Release.

## Considered Options
* Тригер на push тегу `v*`, три послідовні джоби: `create-release` → `build-desktop` (macOS) || `build-android` (Ubuntu)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Тригер на push тегу `v*`, три джоби", because це єдиний описаний варіант; тег як тригер дає чіткий UX — `git tag v0.1.1 && git push origin v0.1.1` → реліз із `.dmg` і `.apk`.

### Consequences
* Good, because transcript фіксує очікувану користь: перший прогін з тегом `v0.1.1` успішно створив реліз і завантажив `mlmail_0.1.0_universal.dmg`.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Файл: `.github/workflows/release.yml` (commit `6db7343`, потім `0e34468`)
- Джоби: `create-release` (`gh release create`), `build-desktop` (`tauri-apps/tauri-action@v0`), `build-android` (`tauri android build --apks --release`)
- Доктрина: `n-ga.mdc` (обовʼязковий `concurrency`, `checkout@v6`, заборона дублювати Bun-setup)

---

## ADR Infisical OIDC замість GitHub Actions Secrets для Android-підпису

## Context and Problem Statement
Android-збірка потребує чотирьох секретів (keystore base64, два паролі, alias). Первинний варіант — зберігати їх у GitHub Actions Secrets напряму — суперечив прийнятому в команді патерну Infisical (doc `docs/android-release-keystore.md`, «Варіант А»), де секрети живуть виключно в Infisical і CI ніколи не отримує їх у вигляді статично збережених GH-секретів.

## Considered Options
* Покласти 4 секрети напряму в GitHub Actions Secrets (`gh secret set`)
* Infisical OIDC machine identity: CI отримує JWT від GitHub → Infisical довіряє → секрети інʼєктуються в env (`Infisical/secrets-action@v1.0.16`, `method: 'oidc'`)

## Decision Outcome
Chosen option: "Infisical OIDC machine identity", because це рекомендований у `docs/android-release-keystore.md` «Варіант А», а також тому що в усіх nitra-репо (nitra/ai) уже застосовано той самий патерн (`method: oidc`, `id-token: write`), що підтвердив transcript після аудиту `gt-run.yml`.

### Consequences
* Good, because transcript фіксує очікувану користь: фінальний прогін підтвердив `Injected 4 secrets from Infisical project into the environment` без жодного секрету, збереженого в GitHub.
* Bad, because transcript фіксує три додаткових кроки налаштування в дашборді Infisical, які CLI не може виконати: (1) OIDC Auth з правильним Subject pattern, (2) додавання identity в проєкт, (3) доступ до secret path; кожна пропущена деталь призводила до окремого 403.

## More Information
- Файл: `.github/workflows/release.yml`, job `build-android`, step `Fetch Android secrets from Infisical`
- `method: 'oidc'`, `domain: 'https://secret.7n.ai'`, `project-slug: 'vitaliytv-kfse'`, `identity-id: '53691c96-17d9-4389-b078-0f77073809ab'`, `env-slug: 'main'`, `secret-path: '/mlmail'`
- OIDC Subject pattern: `repo:{nitra,vitaliytv}/*:ref:refs/{heads/{dev,main},tags/*}` (вимагає охоплення `refs/tags/*` поряд із `refs/heads/*`)
- Commit `6db7343` та наступний перезапис на OIDC

---

## ADR Конфігурація release-підпису в build.gradle.kts з fallback на debug

## Context and Problem Statement
`app/src-tauri/gen/android/app/build.gradle.kts` не мав блоку `signingConfigs` — релізна збірка Android (APK для Google Play / `tauri android build --release`) або не підписувалась, або ламала локальний dev-флоу.

## Considered Options
* Додати `signingConfigs { create("release") { ... } }` з умовним читанням з `keystore.properties` і fallback на `signingConfig = null` (debug-підпис) якщо файл відсутній
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Умовний release-підпис з fallback на debug", because це збереження сумісності локального `tauri android dev`/`build` без CI-змінних, описано як ціль в transcript.

### Consequences
* Good, because transcript фіксує очікувану користь: локальний build не ламається без `keystore.properties`.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Файл: `app/src-tauri/gen/android/app/build.gradle.kts` (mlmail та myshare — однакові правки)
- `keystorePropertiesFile = rootProject.file("keystore.properties")`; якщо файл існує — читаються `storeFile`, `storePassword`, `keyAlias`, `keyPassword`; інакше `signingConfig = null`
- CI-крок `Configure release signing` декодує `ANDROID_KEYSTORE_BASE64` → `keystore.jks` і записує `keystore.properties` безпосередньо перед збіркою

---

## ADR Вилучення rust-cache з release-workflow через cache-poisoning

## Context and Problem Statement
Початкова версія `release.yml` містила `Swatinem/rust-cache@v2` в обох джобах збірки. Zizmor (high-confidence аудит) позначив цей екшен як вектор cache-poisoning у release-контексті.

## Considered Options
* Лишити `rust-cache` (швидша збірка)
* Вилучити `rust-cache` (повільніша збірка, але без аудитового попередження)

## Decision Outcome
Chosen option: "Вилучити `rust-cache`", because zizmor підняв HIGH-рівневе попередження; релізи рідкісні й тривала Rust-компіляція прийнятна без кешу.

### Consequences
* Good, because transcript фіксує очікувану користь: після вилучення `bun run lint-ga` / zizmor перестали фіксувати findings для `release.yml`.
* Bad, because Neutral, because transcript не містить підтвердження наслідку — час збірки Android не виміряно.

## More Information
- Файл: `.github/workflows/release.yml`; видалено кроки `Swatinem/rust-cache@v2` з `build-desktop` і `build-android`
- Замість `softprops/action-gh-release` для upload використано `gh release upload` (теж за рекомендацією zizmor)

---

## ADR Окремий keystore на кожен застосунок

## Context and Problem Statement
Три Tauri-застосунки (mlmail, myshare, task) потребують підпису Android-збірок. Постало питання: один спільний keystore чи по одному на застосунок.

## Considered Options
* Один спільний keystore для всіх застосунків
* Окремий keystore для кожного застосунку

## Decision Outcome
Chosen option: "Окремий keystore для кожного застосунку", because різні Play-лістинги вимагають різних ключів; компрометація одного ключа не скасовує доступ до оновлень інших застосунків (implicit у transcript: генерація виконана роздільно для mlmail і myshare).

### Consequences
* Good, because transcript фіксує очікувану користь: keystore-файли та паролі зберігаються окремо в `~/keystores-release/mlmail/` і `~/keystores-release/myshare/`, alias = назва застосунку.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- `keytool -genkey -keystore mlmail-release.jks -alias mlmail -keyalg RSA -keysize 2048 -validity 10000`
- Аналогічно для myshare з alias `myshare`
- Секрети зберігаються у `~/keystores-release/<app>/secrets/` (поза git-репо)
- task — keystore відкладено до виконання `tauri android init` і комміту `gen/android`
