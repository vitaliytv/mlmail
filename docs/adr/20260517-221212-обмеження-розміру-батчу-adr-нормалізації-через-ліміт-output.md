---
session: 08f98514-3887-4e6a-abe5-0d92e0029504
captured: 2026-05-17T22:12:12+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-vitaliytv-mlmail/08f98514-3887-4e6a-abe5-0d92e0029504.jsonl
---

## ADR Обмеження розміру батчу ADR-нормалізації через ліміт output-токенів Sonnet

## Context and Problem Statement
Скрипт `.claude/hooks/normalize-decisions.sh` за замовчуванням обробляє 30 чернеток за один виклик `claude sonnet`. Dry-run з `ADR_NORMALIZE_BATCH=30` завершився помилкою: відповідь Claude перевищила 32k output-токенів.

## Considered Options
* `ADR_NORMALIZE_BATCH=30` (дефолт) — перевищує ліміт output-токенів
* `ADR_NORMALIZE_BATCH=10` — рекомендований розмір для dry-run

## Decision Outcome
Chosen option: "`ADR_NORMALIZE_BATCH=10` для dry-run", because BATCH=10 дав успішний вихід з коректним планом операцій у межах 32k output-токенів; користувач підтвердив цей вибір через `AskUserQuestion`.

### Consequences
* Good, because transcript фіксує очікувану користь: dry-run з BATCH=10 успішно завершився і повернув коректний план (2 merge-into, 4 delete, 4 rewrite).
* Bad, because transcript не містить підтверджених негативних наслідків, крім того що обробка 33 чернеток тепер вимагає кількох ітерацій.

## More Information
- Команда dry-run: `ADR_NORMALIZE_THRESHOLD=0 ADR_NORMALIZE_MIN_INTERVAL_HOURS=0 ADR_NORMALIZE_BATCH=10 ADR_NORMALIZE_DRY=1 bash .claude/hooks/normalize-decisions.sh`
- Лог: `.claude/hooks/normalize-decisions.log`

---

## ADR Вибір BATCH=5 для реального прогону ADR-нормалізації через таймаут CLI

## Context and Problem Statement
Після успішного dry-run реальний прогон `ADR_NORMALIZE_BATCH=10` завис приблизно на 1 годину й завершився помилкою `Request timed out` у `claude sonnet` CLI.

## Considered Options
* `ADR_NORMALIZE_BATCH=10` повторний прогін (розцінюється як разовий збій)
* `ADR_NORMALIZE_BATCH=5` — менший батч для надійності

## Decision Outcome
Chosen option: "`ADR_NORMALIZE_BATCH=5`", because користувач явно обрав BATCH=5 після таймауту BATCH=10, і прогін успішно завершився (exit code 0) із застосуванням 5 операцій.

### Consequences
* Good, because transcript фіксує: прогін BATCH=5 завершився успішно — 4 delete, 2 merge-into в `ADR-0006-google-oauth.md`, 2 нових rewrite-файли (`oauth-client-id-у-приватному-репо.md`, `oauth-client-ids-runtime-dotenvy.md`).
* Bad, because для обробки 33 чернеток у BATCH=5 потрібно мінімум 7 окремих прогонів.

## More Information
- Команда реального прогону: `ADR_NORMALIZE_THRESHOLD=0 ADR_NORMALIZE_MIN_INTERVAL_HOURS=0 ADR_NORMALIZE_BATCH=5 bash .claude/hooks/normalize-decisions.sh`
- Змінені файли: `docs/adr/ADR-0006-google-oauth.md` (+19 рядків), видалено 5 чернеток, додано `docs/adr/oauth-client-id-у-приватному-репо.md`, `docs/adr/oauth-client-ids-runtime-dotenvy.md`
- Лог: `.claude/hooks/normalize-decisions.log`
