---
session: 08f98514-3887-4e6a-abe5-0d92e0029504
captured: 2026-05-17T20:54:02+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-vitaliytv-mlmail/08f98514-3887-4e6a-abe5-0d92e0029504.jsonl
---

## ADR Обмеження розміру батчу ADR-нормалізації до 10 для Claude Sonnet

## Context and Problem Statement
Хук `normalize-decisions.sh` запускається з дефолтним батчем 30 чернеток. При виконанні dry-run з 31 знайденими чернетками відповідь `claude -p sonnet` перевищила ліміт 32k output-токенів і батч не завершився успішно.

## Considered Options
* Залишити дефолтний батч (30) і повторити
* `ADR_NORMALIZE_BATCH=10` — зменшити до 10 чернеток за один прогін

## Decision Outcome
Chosen option: "`ADR_NORMALIZE_BATCH=10`", because dry-run з 30 файлами впав на 32k output-токенах Sonnet; батч=10 успішно завершив dry-run і сформував коректний план із 10 операцій (2× merge-into, 4× delete, 4× rewrite).

### Consequences
* Good, because transcript фіксує очікувану користь: dry-run з `ADR_NORMALIZE_BATCH=10` завершився з exit code 0 і видав повний план операцій.
* Bad, because при 33 чернетках потрібно кілька послідовних прогонів (≥4) для обробки всього пулу — один прогін не закриває всі drafts.

## More Information
- Команда dry-run: `ADR_NORMALIZE_THRESHOLD=0 ADR_NORMALIZE_MIN_INTERVAL_HOURS=0 ADR_NORMALIZE_BATCH=10 ADR_NORMALIZE_DRY=1 bash .claude/hooks/normalize-decisions.sh`
- Команда реального прогону: `ADR_NORMALIZE_THRESHOLD=0 ADR_NORMALIZE_MIN_INTERVAL_HOURS=0 ADR_NORMALIZE_BATCH=10 bash .claude/hooks/normalize-decisions.sh`
- Лог хука: `.claude/hooks/normalize-decisions.log`
- Перший dry-run (batch=30) — task ID `bn50mqhrp`, завершився з помилкою 32k output.
- Успішний dry-run (batch=10) — task ID `buv4xfiq8`, exit code 0.
