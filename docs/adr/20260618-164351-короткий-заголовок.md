---
session: 1f2895bd-6329-4038-b7c6-4fa942487719
captured: 2026-06-18T16:43:51+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-vitaliytv-mlmail/1f2895bd-6329-4038-b7c6-4fa942487719.jsonl
---

--- diff on clean-merged-branch.yml ---
not modified (pre-existing issue)
pull-requests: write
```
[assistant]
`clean-merged-branch.yml` не змінений, помилка pre-existing. Мій `release.yml` проходить lint та zizmor **чисто**.

Ось що потрібно зробити, щоб реліз запрацював:

---

## Налаштування секретів (одноразово)

**1. Генерація keystore (якщо ще немає):**
```bash
keytool -genkey -v -keystore mlmail-release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias mlmail-release
```

**2. Кодування у base64:**
```bash
base64 -i mlmail-release.jks | pbcopy
```

**3. Додати секрети в GitHub → Settings → Secrets → Actions:**

| Secret | Значення |
|--------|----------|
| `ANDROID_KEYSTORE_BASE64` | вивід base64 з кроку 2 |
| `ANDROID_KEY_ALIAS` | `mlmail-release` (або ваш alias) |
| `ANDROID_KEY_PASSWORD` | пароль ключа |
| `ANDROID_STORE_PASSWORD` | пароль keystore |

---

## Як запустити реліз

```bash
# Оновити версію в tauri.conf.json та app/package.json
git add . && git commit -m "chore: release v0.2.0"
git tag v0.2.0
git push origin v0.2.0
```

GitHub Actions автоматично:
1. Збере **DMG** (universal: `aarch64` + `x86_64`) на macOS-раннері
2. Збере та підпише **APK** (4 ABI) на ubuntu-раннері
3. Створить **GitHub Release** і прикріпить обидва артефакти

---
## ADR <короткий заголовок>

Я помітив, що в `.github/workflows/` ще немає ADR-документа для цього рішення. Це вже зафіксовано у workflow-файлі — потреби в окремому ADR зараз немає, якщо не хочеш.
