---
session: a8164e48-3940-47fa-99f6-2db9a40bb757
captured: 2026-05-22T21:23:50+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-vitaliytv-mlmail/a8164e48-3940-47fa-99f6-2db9a40bb757.jsonl
---

**12/12 правил ✅, 47 тестів ✅.** Ось підсумок того, що зроблено в цій великій сесії.

---

## Якісні метрики покриття — підсумок

### Що встановлено

**Rust mutation testing** — `cargo-mutants v27.0.0` (глобально):

- `app/package.json`: скрипт `test:rust:mutation` (`cargo mutants --manifest-path src-tauri/Cargo.toml`)

**JS/Vue mutation testing** — `@stryker-mutator/core ^9` (devDep у `app`):

- [app/stryker.config.mjs](app/stryker.config.mjs): command-runner на `bun test`, `inPlace: true`, `concurrency: 1`, `timeoutMS: 60000`
- Мутує: `src/services/auth-store.js`, `src/i18n/auth-errors.js`, `src/views/*.vue`, `src/App.vue`
- `app/package.json`: скрипт `test:mutation` (`bunx stryker run`)

**Єдина команда** (`bun run coverage`):

- `scripts/coverage.js` запускає: JS coverage → Rust coverage → JS mutation → Rust mutation
- Уніфікована таблиця у `COVERAGE.md`:

```
| Область | Рядки | Функції | Мутантів | Вбито | Score |
| JS (app) | 99.74% | 100.00% | 106 | 68 | 64.15% |
| Rust | ... | ... | 188 | — | — |
| **Разом** | ... | ... | ... | ... | ...% |
```

### Результати першого Stryker-прогону

- **64.15%** mutation score (68 вбито / 106)
- **38 mutants survived** — слабкі місця в `auth-store.js` (гілки помилок, умовна логіка)
- 0 таймаутів після виправлення конфігу (`concurrency: 1`, `timeoutMS: 60000`)

### Важлива примітка

`bun run coverage` тепер повільна (~8 хв JS + ~20-30 хв Rust). Для щоденних перевірок:

- Тільки coverage: `bun --cwd=app run test:coverage` + `bun --cwd=app run test:rust:coverage`
- Тільки JS mutation: `bun --cwd=app run test:mutation`
- Повний звіт: `bun run coverage` (рідко, перед PR)

Cargo-mutants Rust наразі іще у фоні. Коміт не робив.
