---
session: 96687315-6815-4c7b-9668-a1b1af16f23b
captured: 2026-05-11T22:18:30+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-vitaliytv-mlmail/96687315-6815-4c7b-9668-a1b1af16f23b.jsonl
---

## Секція 2 з 7 — API поверхня Tauri-команд і JS-сторона

**Tauri-команди (Rust, `app/src-tauri/src/auth/mod.rs`):**

```rust
// Кожна команда повертає Result<T, String> (String = серіалізована AuthError)

#[tauri::command]
async fn auth_start_login(app: AppHandle, state: State<Mutex<AuthState>>) -> Result<LoginResult, String>
// → { email: String }  (після успішного обміну кодом)

#[tauri::command]
async fn auth_get_access_token(state: State<Mutex<AuthState>>) -> Result<String, String>
// → "ya29.…"

#[tauri::command]
async fn auth_is_authenticated(state: State<Mutex<AuthState>>) -> bool
// → true/false (синхронна, non-fallible — просто перевірка наявності refresh в пам'яті/сховищі)

#[tauri::command]
async fn auth_logout(state: State<Mutex<AuthState>>) -> Result<(), String>
// Очищає AuthState і видаляє refresh_token зі сховища
// Кличе POST https://oauth2.googleapis.com/revoke?token=<refresh_token> best-effort
```

**JS-сторона (`app/src/services/auth-store.js`, Vue composable):**

```js
// Єдиний стан у пам'яті JS — тільки UI-дані, жодних токенів
const isAuthenticated = ref(false)
const userEmail = ref(null)
const isLoading = ref(false)
const error = ref(null)

async function login() { ... }      // invoke('auth_start_login')
async function logout() { ... }     // invoke('auth_logout')
async function checkAuth() { ... }  // invoke('auth_is_authenticated') — кличеться на App.vue onMounted

// Gmail Client (майбутня робота) буде напряму викликати invoke('auth_get_access_token')
// — не через цей composable

export function useAuth() {
return { isAuthenticated, userEmail, isLoading, error, login, logout, checkAuth }
}
```

**Login.vue (`app/src/views/Login.vue`):**

- Ім'портує `useAuth()`, кнопка "Sign in with Google" кличе `login()`.
- Відображає `error.value` якщо є.
- Після успішного логіну — навігація на головну (майбутній маршрут, поки просто емітить подію або модифікує глобальний стан).

**App.vue зміни:**

- `onMounted` → `checkAuth()` → якщо authenticated — показати головний view; якщо ні — Login.vue (поки через `v-if`, без Vue Router — його немає у проєкті).

Це OK? Якщо так — переходжу до секції 3 (OAuth Google Cloud Console конфігурація і scope).
